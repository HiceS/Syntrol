syntax = "proto3";

package Syntrol;

option csharp_namespace = "Syntrol.Messages";

service Hi {
	rpc HelloReq (Robot) returns (Heartbeat) {}
}

message Robot {
	string id = 1;
	int32 number = 2;
}

message Heartbeat {
	string bump = 1;
}